import React, { Component, PropTypes } from 'react';

import Mytable from "./mytable";



const App = ({ location }) => {

  return (
    <div>
      <Mytable />
    </div>
  );
};

App.propTypes = {
};

export default App;
