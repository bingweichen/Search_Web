import React, { Component, PropTypes } from 'react';
import Mygraph from "./mygraph"

const App = ({ location }) => {

  return (
    <div>
      <Mygraph/>
    </div>
  );
};

App.propTypes = {
};

export default App;
